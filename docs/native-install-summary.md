# Native install summary

DexKeeper's default user-facing install path is native desktop installation.

Recommended paths:

- Windows native app.
- macOS native app.
- Linux AppImage.

DexKeeper stores runtime data locally on the user's machine. Docker is optional for advanced operators and is not required for normal use.

Developer and service examples live under `scripts/` and `packaging/`.
